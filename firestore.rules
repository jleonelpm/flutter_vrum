rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Desarrollo: acceso abierto con emulador + bearer owner
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
